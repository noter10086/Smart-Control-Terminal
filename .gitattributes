# 换行符策略
# 仓库内统一以 LF 存储，避免不同平台协作时产生「整文件都变了」的伪 diff。
# 分支规则放最后（越具体越靠后）会覆盖上面的通配规则。

# 默认：由 Git 自动判定是否为文本文件并纳入规范化
* text=auto

# ===== Keil MDK 工程文件：强制 CRLF =====
# Keil 对工程文件的换行格式较敏感，统一 CRLF 避免跨平台打开时出现异常
*.uvprojx    text eol=crlf
*.uvoptx     text eol=crlf
*.uvproj     text eol=crlf
*.sct        text eol=crlf

# ===== STM32CubeMX 工程配置 =====
*.ioc        text eol=crlf
.mxproject   text eol=crlf

# ===== 启动汇编文件：保持 CRLF =====
*.s          text eol=crlf

# ===== C 源码与头文件：统一 LF =====
*.c          text eol=lf
*.h          text eol=lf

# ===== 二进制与编译产物：禁止任何换行转换 =====
# （多数已被 .gitignore 排除，此处为双重保险，防止误加入时被 Git 破坏）
*.o      binary
*.d      binary
*.crf    binary
*.axf    binary
*.elf    binary
*.hex    binary
*.bin    binary
*.map    binary
*.lst    binary
*.lib    binary
*.a      binary

# ===== 媒体资源 =====
*.png    binary
*.jpg    binary
*.jpeg   binary
*.gif    binary
*.ico    binary
*.pdf    binary
